<form method="post" action="{{ path('ronda_torneo_delete', {'id': ronda_torneo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ronda_torneo.id) }}">
    <button class="btn">Delete</button>
</form>