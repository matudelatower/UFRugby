{% extends 'base.html.twig' %}

{% block title %}
    Club
{% endblock title %}
{% block body %}
    <h1>Editar Club</h1>
    <div class="box">
        <div class="box-header">
            <h3 class="box-title">Club</h3>
        </div>
        <div class="box-body">

            {{ form_start(edit_form) }}
            {{ form_widget(edit_form) }}
            <input class="btn btn-primary btn-guardar" type="submit" value="Guardar"/>
            {{ form_end(edit_form) }}
        </div>
    </div>

    <a class="btn btn-default" href="{{ path('club_index') }}">Volver</a>



{% endblock %}
