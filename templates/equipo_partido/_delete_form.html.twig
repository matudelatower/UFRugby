<form method="post" action="{{ path('equipo_partido_delete', {'id': equipo_partido.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ equipo_partido.id) }}">
    <button class="btn">Delete</button>
</form>