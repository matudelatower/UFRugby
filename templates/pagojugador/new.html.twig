{% extends 'base.html.twig' %}

{% block title %}
Pago jugador
{% endblock title %}
{% block body %}
    <h1>Nuevo Pago Jugador</h1>
    <div class="card">
    <div class="card-header">
        <h3 class="card-title">Datos del Pago</h3>
    </div>
    <div class="card-body">

        {{ form_start(form) }}
            {{ form_widget(form) }}
            <input class="btn btn-primary" type="submit" value="Crear" />
        {{ form_end(form) }}
    </div>
    </div>


<a class="btn btn-default" href="{{ path('pagojugador_index') }}">Volver</a>

{% endblock %}
