<h1>Hola {{ user }}!</h1>

<p>Para restablecer tu contraseña por favor hacé click en el siguiente enlace</p>

<a href="{{ url('app_reset_password', {token: resetToken.token}) }}">{{ url('app_reset_password', {token: resetToken.token}) }}</a>

<p>El enlace expira en {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>

<p>Atentamente, {{ union_name }}.</p>
