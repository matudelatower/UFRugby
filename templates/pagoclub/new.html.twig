{% extends 'base.html.twig' %}

{% block title %}
    Pago club
{% endblock title %}
{% block body %}
    <h1>Registrar pago de Club</h1>
    <div class="card">
        <div class="card-header">
            <h3 class="card-title">Pago club</h3>
        </div>
        <div class="card-body">

            {{ form_start(form) }}
            {{ form_widget(form) }}
            <input class="btn btn-primary" type="submit" value="Crear"/>
            {{ form_end(form) }}
        </div>
    </div>


    <a class="btn btn-default" href="{{ path('pagoclub_index') }}">Volver</a>

{% endblock %}
