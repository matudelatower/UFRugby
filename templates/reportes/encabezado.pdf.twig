<!doctype html>
<html lang="es">
<head>
    <meta charset="UTF-8">
</head>
<link rel="stylesheet" href="{{ absolute_url(asset('build/css/main.css')) }}">
<body>
<div class="container-fluid">
    <div class="row">
        <div class="col-xs-4">
            <div style="width: 33%">
                <img class="img-responsive" src="{{ absolute_url(asset('build/img/logo_urp.jpg')) }}">
            </div>
        </div>
        <div class="col-xs-4 text-center">
            <h3>Unión de Rugby del Paraguay</h3>
        </div>
        <div class="col-xs-4">
            <div class="float-right">
                <strong>Usuario:</strong> {{ app.user }}<br>
                {% if app.user.club %}
                    <strong>Club:</strong> {{ app.user.club }}
                {% endif %}
            </div>
        </div>
    </div>
</div>
</body>
</html>